version: "3.8"

services:
  teamhub:
    build: .
    ports:
      - "3000:3000"
    restart: unless-stopped
    labels:
      - "howl.target=mass-assignment"
      - "howl.difficulty=easy"
    environment:
      - FLASK_ENV=production
