version: "3.8"

services:
  financeapi:
    build: .
    ports:
      - "3000:3000"
      - "8080:8080"
    restart: unless-stopped
    labels:
      - "howl.target=cors-misconfiguration"
      - "howl.difficulty=medium"
    environment:
      - FLASK_ENV=production
