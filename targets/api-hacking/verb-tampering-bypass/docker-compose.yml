version: "3.8"

services:
  configapi:
    build: .
    ports:
      - "3000:3000"
    restart: unless-stopped
    labels:
      - "howl.target=verb-tampering-bypass"
      - "howl.difficulty=medium"
    environment:
      - FLASK_ENV=production
